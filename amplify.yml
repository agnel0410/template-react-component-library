version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm cache clean --force
        - npm i
    build:
      commands:
        - npm run build-sb
  artifacts:
    baseDirectory: dist-storybook
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*