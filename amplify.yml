version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm --version
        - npm ci
    build:
      commands:
        - npm run build-sb
  artifacts:
    baseDirectory: dist-storybook
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*